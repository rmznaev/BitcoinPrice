platform :ios, '9.0'

target 'BitcoinPrice' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BitcoinPrice

    pod 'SwiftyJSON'
    pod 'Alamofire'

end
